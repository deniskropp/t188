# How does visualizing the Knowledge Graph aid in verification?

The `/graph` command dumps a table of all nodes, types, and descriptions, providing a "source of truth" independent of the narrative text.

This allows users (directors) to verify critical details—such as whether a specific item truly exists in the world or if a character is legally present in a scene—without having to parse back through the story text or rely on memory.
